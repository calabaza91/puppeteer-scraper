/**
 * Web Scraper for books
 */

//Requirements
const puppeteer = require('puppeteer')
const { url } = require('./config')

;
(async() => {
    // Open Browser

    // Setup Browser

    // Close Browser

})().catch(error => {
    console.log(error)
    process.exit(1)
})